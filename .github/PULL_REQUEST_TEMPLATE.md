## 概要

<!-- 何を、なぜ変更したか -->

## 検証

- [ ] `dart analyze`（core） / `flutter analyze`（desktop）が通る
- [ ] `dart test`（core） / `flutter test`（desktop）が通る
- [ ] `dart pub get --enforce-lockfile` が通る（lockfile とバージョンが一致）
- [ ] UI の変更がある場合、実機で動作確認した
