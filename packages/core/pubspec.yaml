name: moost_core
description: >-
  Core logic for Moost — session memos and one-click resume for
  AI coding agent CLIs (Claude Code first).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

# ランタイム依存ゼロを維持する（Swift 版と同方針。design.md 8 章参照）
dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
